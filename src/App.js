import React from "react";
import styled from "styled-components";

const App = () => {
  return <AppWrapper>React Highcharts</AppWrapper>;
};

const AppWrapper = styled.div`
  text-align: center;
`;

export default App;
